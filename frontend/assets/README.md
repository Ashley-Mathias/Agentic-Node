# Assets
<!-- frontend/assets/ — images and static files referenced by CSS/HTML -->

Place the **Agentic Node logo image** (the one with the node icon and "AGENTIC NODE" text on dusty rose background) here as:

- **`agentic-node-bg.png`** – used as the app background in `css/base.css`.

If your file has a different name, either rename it to `agentic-node-bg.png` or update the `background-image` URL in `frontend/css/base.css` to match.
